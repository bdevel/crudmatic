<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Library Management - Crudable Demo" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    
    <!-- Bootstrap CSS for styling -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <%= stylesheet_link_tag "application" %>
  </head>

  <body>
    <!-- Navigation Header -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
      <div class="container">
        <%= link_to "📚 Library Management", root_path, class: "navbar-brand" %>
        
        <div class="navbar-nav">
          <%= link_to "Books", books_path, class: "nav-link #{'active' if controller_name == 'books'}" %>
          <%= link_to "Authors", authors_path, class: "nav-link #{'active' if controller_name == 'authors'}" %>
          <%= link_to "Categories", categories_path, class: "nav-link #{'active' if controller_name == 'categories'}" %>
        </div>
      </div>
    </nav>

    <!-- Flash Messages -->
    <div class="container mt-3">
      <% flash.each do |type, message| %>
        <div class="alert alert-<%= type == 'notice' ? 'success' : 'danger' %> alert-dismissible fade show" role="alert">
          <%= message %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% end %>
    </div>

    <!-- Page Title -->
    <div class="container mt-3">
      <div class="d-flex justify-content-between align-items-center mb-4">
        <h1><%= page_title %></h1>
        <small class="text-muted">Powered by Crudable Engine</small>
      </div>
    </div>

    <!-- Main Content -->
    <div class="container">
      <%= yield %>
    </div>

    <!-- Footer -->
    <footer class="mt-5 py-4 bg-light text-center">
      <div class="container">
        <p class="text-muted mb-0">
          <strong>Library Management System</strong> - 
          Demonstrating the <em>Crudable Rails Engine</em>
        </p>
        <small class="text-muted">
          No views duplicated - all CRUD functionality provided by the engine!
        </small>
      </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
