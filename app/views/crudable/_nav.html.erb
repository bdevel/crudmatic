<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
      <% if request.path != index_path(record) %>
        <li class="nav-item">
          <%= link_to 'javascript: window.history.back()', class: 'nav-link' do %>
            <i class="bi bi-chevron-left"></i>
            Back
          <% end %>
        </li>
      <% end %>
      <li class="nav-item">
        <%= link_to index_path(record), class: "nav-link #{active_nav_item_class(index_path(record))}" do %>
          <i class="bi bi-list-ul"></i>
          View All
        <% end %>
      </li>

      <% if action_ok?(local_assigns[:actions], :create) %>
        <li class="nav-item">
          <%= link_to new_path(record), class: "nav-link #{active_nav_item_class(new_path(record))}" do %>
            <i class="bi bi-plus-lg"></i>
            New
          <% end %>
        </li>
      <% end %>

      <% if record && action_ok?(local_assigns[:actions], :show) && request.path != index_path(record) && request.path != new_path(record) %>
        <li class="nav-item">
          <%= link_to view_path(record), class: "nav-link #{active_nav_item_class(view_path(record))}" do %>
            <i class="bi bi-eye"></i>
            View
          <% end %>
        </li>
      <% end %>
            
      <% if record && action_ok?(local_assigns[:actions], :edit) %>
        <% if request.path != index_path(record) && request.path != new_path(record) %>
          <li class="nav-item">
            <%= link_to edit_path(record), class: "nav-link #{active_nav_item_class(edit_path(record))}" do %>
              <i class="bi bi-pencil"></i>
              Edit
            <% end %>
          </li>
        <% end %>
      <% end %>
      <% if record && action_ok?(local_assigns[:actions], :delete) %>
        <% if request.path != index_path(record) && request.path != new_path(record) %>
          <li class="nav-item">
            <%= link_to record, data: { confirm: "Are you sure you want to delete #{record}?" }, method: :delete, class: 'nav-link' do %>
              <i class="bi bi-trash"></i>
              Delete
            <% end %>
          </li>
        <% end %>
      <% end %>
        <% begin %>
          <%= render partial: "#{controller.to_s.underscore.gsub(' ', '_')}/nav_links_extra", locals: {record: record} %>
        <% rescue ActionView::MissingTemplate => e %>
        <% end %>
      </ul>
      
      <% begin %>
        <!-- render partial: "#{record.model_name.plural}/nav_extra", locals: {record: record} -->
        <%= render partial: "#{controller.to_s.underscore.gsub(' ', '_')}/nav_extra", locals: {record: record} %>
      <% rescue ActionView::MissingTemplate => e %>
      <% end %>

      <% if can_search?(record) %>
        <%= render partial: 'crudable/search' %>
      <% end %>
    </div>
  </div>
</nav>
